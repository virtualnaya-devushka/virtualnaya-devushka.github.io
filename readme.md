# Лендинг "Нейрофотосессия онлайн"

Одностраничный сайт с рейтингом нейросетей. Размещен на GitHub Pages.
**Стек:** HTML5, CSS3 (без библиотек), адаптивная верстка.

## ⚙️ Инструкция по редактированию

Все изменения вносятся в файл `index.html`.

1. **Ссылки на сервисы:**  
   Откройте файл, нажмите `CTRL+F` и найдите текущую ссылку (например, `linkwork.pro`). Замените значение внутри кавычек `href="..."` на новое.

2. **Тексты:**  
   Текст находится внутри тегов `<p>Текст</p>`, `<h1>Заголовок</h1>` или `<li>Пункты</li>`. Просто меняйте слова между тегами.

3. **Яндекс.Метрика:**  
   Найдите в коде строку `ym(106452280`. Замените цифры `106452280` на новый ID вашего счетчика (встречается 2 раза).

4. **Картинки:**  
   Лежат в папке `img/`.  
   Формат: **.webp**.  
   Имена файлов: `1.webp`, `2.webp` и т.д.  
   Для замены просто загрузите новые файлы с теми же именами в папку `img`.

## 🚀 Запуск
Сайт работает автоматически через GitHub Pages.
